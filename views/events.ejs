<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="Current and future experiences from <%= COMPANY_NAME %>.">
  <meta property="og:title" content="Asile Events">
  <meta property="og:description" content="Discover current and future experiences from <%= COMPANY_NAME %>.">
  <meta property="og:image" content="<%= SITE_IMAGE_URL %>">
  <meta property="og:type" content="website">
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="Asile Events">
  <meta name="twitter:description" content="Discover current and future experiences from <%= COMPANY_NAME %>.">
  <meta name="twitter:image" content="<%= SITE_IMAGE_URL %>">
  <title>Asile Events</title>
  <link rel="icon" type="image/png" sizes="512x512" href="/public/favicon.png?v=20260705a">
  <link rel="apple-touch-icon" sizes="512x512" href="/public/favicon.png?v=20260705a">
  <link rel="preload" href="/public/vineyard-sunset.svg" as="image">
  <link rel="stylesheet" href="/public/style.css?v=20260715d">
</head>
<body class="events-body asile-public-page">
  <header class="events-nav">
    <a class="events-brand" href="/" aria-label="Return to <%= COMPANY_NAME %>">
      <img src="/public/favicon.png?v=20260705a" alt="<%= COMPANY_NAME %>">
      <span><%= COMPANY_NAME %><small>Events</small></span>
    </a>
    <nav aria-label="Asile links">
      <a href="/">Current event</a>
      <a href="<%= WAITLIST_PATH %>">Priority List</a>
      <a href="<%= INSTAGRAM_URL %>" target="_blank" rel="noopener">Instagram</a>
    </nav>
  </header>

  <main class="events-hub">
    <section class="events-intro">
      <p class="eyebrow">Asile Events</p>
      <h1>Curated nights, made to be remembered.</h1>
      <p>Explore the current Asile experience or join Priority List for first access to the next one.</p>
    </section>

    <section class="events-section" aria-labelledby="current-event-heading">
      <div class="events-section-heading">
        <div>
          <p class="eyebrow">Current experience</p>
          <h2 id="current-event-heading"><%= EVENT_NAME %></h2>
        </div>
        <span class="event-state <%= ticketAvailability.soldOut ? 'event-state-sold-out' : '' %>"><%= ticketAvailability.soldOut ? 'Sold out' : `${ticketAvailability.remaining} tickets left` %></span>
      </div>

      <article class="event-directory-card">
        <div class="event-directory-image">
          <img src="<%= DJ_IMAGE_URL %>" alt="<%= DJ_NAME %>" decoding="async">
          <span><%= EVENT_THEME %></span>
        </div>
        <div class="event-directory-content">
          <p class="event-directory-kicker"><%= EVENT_DATE %> · <%= EVENT_TIME %></p>
          <h3><%= EVENT_NAME %></h3>
          <p>An <%= DRESS_CODE.toLowerCase() %> sunset gathering at <%= EVENT_LOCATION %>, with house music from <%= DJ_NAME %>, named QR entry, and an approved guest list.</p>
          <dl class="event-directory-facts">
            <div><dt>Location</dt><dd><%= EVENT_LOCATION %></dd></div>
            <div><dt>Entry</dt><dd><%= MIN_AGE %>+</dd></div>
            <div><dt>Dress</dt><dd><%= DRESS_CODE %></dd></div>
            <div><dt>Tickets</dt><dd><%= ticketAvailability.soldOut ? 'Sold out' : money(TICKET_PRICE) %></dd></div>
          </dl>
          <div class="event-directory-actions">
            <a class="button <%= ticketAvailability.soldOut ? 'sold-out-button' : '' %>" href="/#reserve"><%= ticketAvailability.soldOut ? 'Event sold out' : 'Reserve tickets' %></a>
            <a class="button secondary" href="<%= MAP_URL %>" target="_blank" rel="noopener">Open location</a>
          </div>
        </div>
      </article>
    </section>

    <section class="events-priority" aria-labelledby="future-events-heading">
      <div>
        <p class="eyebrow">What is next</p>
        <h2 id="future-events-heading">First access to future Asile events.</h2>
        <p>Priority List is for people who want to hear about the next experience before tickets are released publicly. It can also be used when spaces become available for a sold-out event.</p>
      </div>
      <div class="events-priority-actions">
        <a class="button" href="<%= WAITLIST_PATH %>">Join Priority List</a>
        <a class="text-link" href="<%= INSTAGRAM_URL %>" target="_blank" rel="noopener">Follow Asile on Instagram</a>
      </div>
    </section>

    <% if (futureEvents.length) { %>
      <section class="future-events" aria-labelledby="future-events-list-heading">
        <div class="events-section-heading">
          <div>
            <p class="eyebrow">Coming up</p>
            <h2 id="future-events-list-heading">Future experiences</h2>
          </div>
        </div>
        <div class="future-events-grid">
          <% futureEvents.forEach(event => { %>
            <article class="future-event-card">
              <% if (event.imageUrl) { %><img src="<%= event.imageUrl %>" alt="<%= event.name %>" loading="lazy" decoding="async"><% } %>
              <p><%= event.eventDate %><% if (event.eventTime) { %> · <%= event.eventTime %><% } %></p>
              <h3><%= event.name %></h3>
              <span><%= event.location %></span>
              <% if (event.description) { %><div><%= event.description %></div><% } %>
              <a class="text-link" href="<%= WAITLIST_PATH %>">Join Priority List</a>
            </article>
          <% }) %>
        </div>
      </section>
    <% } %>
  </main>
</body>
</html>
